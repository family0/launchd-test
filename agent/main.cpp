#include <iostream>
#include <stdlib.h>
#include <sys/stat.h>

using namespace std;

int main() {
    cout << "agent START" << endl;
    // mkdir("dir1", 0644);
    // system("launchctl bootstrap system /Library/LaunchDaemons/kr.itst.prs5updater.plist");
    return 0;
}